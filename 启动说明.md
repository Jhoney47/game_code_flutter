# Flutter应用启动说明

## 📱 Android APK安装

### 方式1：直接下载APK（推荐）
```
https://github.com/Jhoney47/game_code_flutter/releases/latest
```

下载arm64版本的APK，在Android手机上直接安装即可。

---

## 🖥️ 本地开发运行

### 前提条件
1. 安装Flutter SDK（3.24.0或更高版本）
2. 安装Android Studio或VS Code
3. 配置Android SDK

### 启动命令

#### 1. 安装依赖
```bash
cd /path/to/flutter_standalone
flutter pub get
```

#### 2. 运行在Android设备/模拟器
```bash
# 连接Android设备或启动模拟器
flutter devices

# 运行应用（调试模式）
flutter run

# 运行应用（发布模式）
flutter run --release
```

#### 3. 运行在Web浏览器
```bash
flutter run -d chrome
```

#### 4. 构建APK
```bash
# 构建所有架构的APK
flutter build apk --release --split-per-abi

# 构建单个APK（包含所有架构）
flutter build apk --release

# 输出目录
# build/app/outputs/flutter-apk/
```

---

## 🚀 GitHub Actions自动打包

### 自动打包（推送tag触发）
```bash
# 创建版本tag
git tag v1.0.4
git push origin v1.0.4

# GitHub Actions自动打包并创建Release
# 5-10分钟后在Releases页面下载APK
```

### 手动打包
1. 访问：https://github.com/Jhoney47/game_code_flutter/actions
2. 选择"手动打包APK"工作流
3. 点击"Run workflow"
4. 输入版本号并运行

---

## 🔧 后台服务启动（如果有后端）

**注意：** 当前Flutter应用是纯前端应用，不需要后台服务。数据直接从GitHub CDN获取。

如果未来需要添加后端服务，可以参考以下方式：

### Node.js后端
```bash
cd server
npm install
npm start
```

### Python后端
```bash
cd server
pip install -r requirements.txt
python app.py
```

---

## 📊 数据源

应用数据来自GitHub仓库，通过jsDelivr CDN加速访问：

```
https://cdn.jsdelivr.net/gh/Jhoney47/GameCodeBase@main/GameCodeBase.json
```

**更新数据：**
1. 修改GameCodeBase仓库中的GameCodeBase.json文件
2. 提交并推送到GitHub
3. 应用会自动获取最新数据（CDN缓存约5分钟）

---

## 🐛 调试

### 查看日志
```bash
# 实时查看应用日志
flutter logs

# 查看设备日志
adb logcat | grep flutter
```

### 清除缓存
```bash
flutter clean
flutter pub get
```

### 重新生成代码
```bash
# 如果修改了JSON模型
flutter pub run build_runner build --delete-conflicting-outputs
```

---

## 📦 项目结构

```
flutter_standalone/
├── lib/
│   ├── main.dart                 # 应用入口
│   ├── screens/
│   │   ├── splash_screen.dart    # 启动页
│   │   ├── home_screen.dart      # 主页
│   │   └── code_detail_screen.dart # 详情页
│   ├── models/
│   │   ├── game_code.dart        # 数据模型
│   │   └── game_code.g.dart      # 生成的序列化代码
│   ├── repositories/
│   │   └── code_repository.dart  # 数据仓库
│   ├── widgets/
│   │   └── code_card.dart        # 兑换码卡片组件
│   └── theme/
│       └── app_theme.dart        # 主题配置
├── android/                      # Android配置
├── pubspec.yaml                  # 依赖配置
└── .github/workflows/            # GitHub Actions配置
```

---

## 🎯 常见问题

### Q: 应用启动后白屏？
A: 检查网络连接，确保能访问GitHub CDN。

### Q: 数据加载失败？
A: 
1. 检查网络连接
2. 确认GameCodeBase.json文件格式正确
3. 查看应用日志获取详细错误信息

### Q: 如何修改应用名称？
A: 修改`android/app/src/main/AndroidManifest.xml`中的`android:label`

### Q: 如何修改应用图标？
A: 替换`android/app/src/main/res/mipmap-*/ic_launcher.png`文件

---

## 📞 技术支持

- **GitHub仓库：** https://github.com/Jhoney47/game_code_flutter
- **数据仓库：** https://github.com/Jhoney47/GameCodeBase
- **Issues：** https://github.com/Jhoney47/game_code_flutter/issues

---

**最后更新：** 2026-01-21
