# 游戏码宝 Flutter版 - 快速开始指南

## 🚀 5分钟快速部署

### 第一步：安装Flutter环境

#### Windows用户
1. 下载Flutter SDK：https://docs.flutter.dev/get-started/install/windows
2. 解压到 `C:\flutter`
3. 添加到环境变量：`C:\flutter\bin`
4. 运行 `flutter doctor` 检查环境

#### macOS用户
```bash
# 使用Homebrew安装
brew install flutter

# 检查环境
flutter doctor
```

#### Linux用户
```bash
# 下载Flutter
wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.16.0-stable.tar.xz

# 解压
tar xf flutter_linux_3.16.0-stable.tar.xz

# 添加到PATH
export PATH="$PATH:`pwd`/flutter/bin"

# 检查环境
flutter doctor
```

### 第二步：安装Android Studio（打包APK必需）

1. 下载Android Studio：https://developer.android.com/studio
2. 安装Android SDK
3. 创建Android虚拟设备（AVD）或连接真机

### 第三步：运行项目

```bash
# 进入项目目录
cd game_code_flutter

# 安装依赖
flutter pub get

# 运行应用（自动检测设备）
flutter run

# 或指定设备运行
flutter run -d <device-id>

# 查看可用设备
flutter devices
```

### 第四步：打包APK

```bash
# 打包Release版本APK
flutter build apk --release

# APK位置
# build/app/outputs/flutter-apk/app-release.apk
```

### 第五步：分发给用户

将生成的 `app-release.apk` 发送给用户，用户在Android手机上直接安装即可。

## 📱 测试步骤

### 1. 测试国内CDN访问
- 打开应用，观察是否能正常加载数据
- 查看控制台日志，确认使用jsDelivr CDN
- 预期：数据加载速度快，无需VPN

### 2. 测试下拉刷新
- 在首页下拉
- 观察是否显示加载动画
- 确认数据更新成功提示

### 3. 测试动态Tabs
- 观察顶部游戏标签
- 确认标签数量与JSON中的游戏数量一致
- 点击不同标签，确认筛选正确

### 4. 测试搜索功能
- 在搜索框输入游戏名称或兑换码
- 确认搜索结果正确
- 清空搜索框，确认恢复所有数据

### 5. 测试排序和筛选
- 点击"最新发布"、"可信度最高"、"即将过期"
- 确认排序逻辑正确
- 点击"永久"、"限时"筛选
- 确认筛选结果正确

### 6. 测试详情页
- 点击任意兑换码卡片
- 进入详情页
- 点击"复制兑换码"按钮
- 确认复制成功提示
- 粘贴到其他应用，确认内容正确

### 7. 测试状态显示
- 观察有效兑换码：绿色高亮，正常透明度
- 观察过期兑换码：灰色置灰，降低透明度
- 确认可信度进度条显示正确

## 🔧 常见问题排查

### 问题1：flutter command not found
**解决：** 确认Flutter已添加到PATH环境变量

```bash
# 检查Flutter路径
which flutter

# 如果没有输出，需要添加到PATH
export PATH="$PATH:/path/to/flutter/bin"
```

### 问题2：Android licenses not accepted
**解决：** 运行以下命令接受许可证

```bash
flutter doctor --android-licenses
```

### 问题3：数据加载失败
**解决：** 
1. 检查网络连接
2. 确认jsDelivr CDN可访问
3. 查看控制台错误日志

### 问题4：APK安装失败
**解决：**
1. 确认Android版本 >= 5.0
2. 在设置中允许"未知来源"安装
3. 检查APK文件是否完整

## 📊 性能优化建议

### 1. 减小APK体积
```bash
# 使用split-per-abi打包
flutter build apk --split-per-abi

# 生成多个APK，每个对应不同CPU架构
# arm64-v8a (64位，推荐)
# armeabi-v7a (32位)
# x86_64 (模拟器)
```

### 2. 启用混淆
```bash
flutter build apk --release --obfuscate --split-debug-info=./debug-info
```

### 3. 优化图片资源
- 使用WebP格式
- 压缩图片大小
- 使用适当分辨率

## 🌐 部署到应用商店

### Google Play Store
1. 注册Google Play开发者账号（$25一次性）
2. 创建应用
3. 上传APK或AAB
4. 填写应用信息
5. 提交审核

### 华为应用市场（推荐国内）
1. 注册华为开发者账号
2. 创建应用
3. 上传APK
4. 填写应用信息
5. 提交审核

### 小米应用商店（推荐国内）
1. 注册小米开发者账号
2. 创建应用
3. 上传APK
4. 填写应用信息
5. 提交审核

## 📞 技术支持

如遇到问题，请检查：
1. Flutter版本是否 >= 3.0.0
2. Dart版本是否 >= 3.0.0
3. 依赖是否正确安装（flutter pub get）
4. 网络连接是否正常
5. jsDelivr CDN是否可访问

## 🎉 完成！

恭喜您成功部署游戏码宝Flutter版！现在您可以：
- ✅ 打包APK分发给用户
- ✅ 发布到应用商店
- ✅ 通过后台更新JSON，前端自动同步
- ✅ 国内用户无需VPN即可使用
